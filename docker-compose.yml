services:
  wt2xt:
    image: wt2xt
    build:
      context: .
    ports:
      - "3002:3000"
    volumes:
      - ./templates:/app/templates
      - ./out:/app/out:rw
      - ./tmp:/app/tmp:rw
      - ./config:/app/config
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      # Docker Engine over Unix socket
      - DOCKER_HOST=unix:///var/run/docker.sock
      # Capture the host user’s UID/GID for correct file ownership in one‑shot containers
      - HOST_UID=${HOST_UID}
      - HOST_GID=${HOST_GID}
      # The host directory you want to mount as /data in the pandoc container
      - HOST_WORKDIR=
    env_file:
      - .env
    networks:
      - frontend
    # Run the API server by default
    command: api
    # Run as root to ensure access to /var/run/docker.sock mounted from host
    user: "0:0"

    # To run the CLI tool instead, use:
    # docker-compose run --rm wt2xt --web-template=./templates/example.json --export-format=adoc

    # Ensure host directories exist with proper permissions before starting
    # This helps with Linux permission issues


networks:
  frontend:
    external: true
